import React from "react";
import ReactDOM from "react-dom/client";
import EndoGuard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EndoGuard />
  </React.StrictMode>,
);
